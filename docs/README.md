# Cluster docs

This repository is **Flux YAML**. Gitea is origin; GitHub is a mirror.

Canonical docs and scripts: `~/jarvis-infra/`
(`docs/COPILOT.md`, `PLAN.md`, `OPERATING.md`, `INTERACT.md`, `REBUILD.md`, `RESTORE.md`, `LESSONS.md`).

Pins: `~/jarvis-infra/VERSION`. Do not copy git/image tags here.
